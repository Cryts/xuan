# 《玄》项目契约

> 最近更新：2026-09-20 18:25
> 本文件是**静态契约与绝对规则**。动态进展见 `PROJECT_STATE.md`，设计依据见 `docs/superpowers/specs/`。

---

## 一、🔒 永久规则（最高优先级，不得软化、不得绕过）

### 设计方向

- [ ] **自由度是《玄》的核心卖点。** 任何新增功能必须回答"它增加了哪一类自由度"；增加不了就别做。
- [ ] **情节必须自由化与限制化结合。** 不能只有自由（会崩）也不能只有限制（会僵）。限制是玩法的语法，不是自由的敌人。
- [ ] **不做小白文。** 要《惊悚乐园》那种**智性**的封闭剧本设计——靠理解规则破局，不是靠数值碾压一路爽。反套路、信息不对称、字面漏洞是常规手段。
- [ ] **方案永远不锁定。** 每一版都留扩展位（剧本链、物品组合、位面之子互相倾轧、体系包扩容到 20+）。不要焊死接口。
- [ ] **必须有 AIGC 的体现。** 纯模板会砍掉自由度。用 L2 预生成池（构建期由子 agent 批量产出并审核）+ 可选 L3 实时；不能退化成"只有几十条手写模板"。
- [ ] **剧本不能全是解谜。** 要有多类型：逐斗 / 逃亡 / 守御 / 渡劫 / 博弈 / 规则压制……仙侠与科幻里有的都可能有。每次都和解密一样太单调。
- [ ] **剧本通关条件要随玩家进度提高。** 不是固定值。
- [ ] **剧本要依据玩家物品给关联性的创新解法**——但**不是所有物品在哪个副本都说得通**，必须有关联。无关的物件不该能硬套。
- [ ] **剧本前文必须给隐藏提示**：暗示哪些选项能提高这一局的通关几率。
- [ ] **前面的选项与成败必须给玩家剧本相关的道具**，且**成败给的东西不同**。

### 战斗与物品

- [ ] **斗法 = 明牌比试 + 路数对抗。** 明牌负责斗法前的选择与给后续上 buff；路数对抗负责实际攻防。**斗法可以杀死对方，但有相应的奖惩**（可能只有奖励、可能两者都有）。
- [ ] **斗法是奇遇不是日常。** 遭遇时先给「出手 / 避开」，出手不推进节点。
- [ ] **物品分两类**：`daily` 日常物品（**可随时使用**）/ `key` 剧本关键物品（只在局里使得上）。分类由功能标签派生，不靠人标。
- [ ] **修为不是每次事件都增长**，且要与处境挂钩（有时快有时慢，受伤可倒退，可有暂时增强）。自身状态、灵石等都要与特定事件相挂钩。

### 安全与合规

- [ ] **IP 审核是硬门禁。** 禁用词命中即 build fail，不可绕过。借鉴"规则与结构"，不使用"独创表达"。
- [ ] **数值永不由模型决定。** 模型只碰表达层；任何"获得 X""突破成功"必须由规则层算出并落日志。

### 工程纪律

- [ ] **不许说"做好了"而没有证据。** 关键改动必须跑门禁并贴出输出。这个项目已经出过多次"所有测试全绿但功能是死的"（结局正文全缺、浏览器加载器跑兜底内容、`add_item` 空操作、剧本属性门槛不可达、选项预告 1629 条全空）。
- [ ] **发现是真的缺陷，先报告再改，不许默默改绿。**
- [ ] **测试必须稳定。** 会随机变绿的测试比没有测试更糟——它让人以为验过了。用固定种子。
- [ ] **加功能必须同时加闸门。** 每修好一类静默失效，就要在 `tools/content-lint.ts` 里加一条规则，让它不能再回来。
- [ ] **子 agent 交付后要验收，未完成的要盯。** 并行的 agent 之间会互相覆盖，动手前先看文件时间戳。
- [ ] **测量的工具比被测的东西更值得先检查。** 试玩 agent 空了三百局没报警，是因为它把 `__daily__` / `__duel__` 当成"卡死"跳过了 —— **一个瞎了的仪表会把结论一起带偏**，而且比坏功能更难发现。改动工具后要反问：它还能看见真实行为吗？
- [ ] **能派生就不要手写。** 一千六百条手写文案会腐化（内容改了文案不改，预告开始撒谎），而说得不准比不说更糟。凡是有唯一真相源（效果、条件、数值）的展示文案，一律从真相源派生，作者文案只作覆盖。
- [ ] **提示必须与行为对得上。** 界面上写着"伤势略增"而实际在减，比数值偏一点糟得多 —— 它让玩家不再相信界面上写的任何代价。改行为要顺手改提示。
- [ ] **门禁阈值不能细于指标自身的噪声。** 支配性比值在 400 局时 ±6%、2400 局时 ±2.3%；拿 20.1% 与 20% 比是在读噪声。判定要看置信下界，读数落在噪声里就**明说测不准**，不要假装量准了。
- [ ] **终局条件要按相对位置说，不按绝对序号说。** 各体系包阶梯长短不一（10 层 / 13 层 / 18 层），同一个 `realm_idx >= 9` 在短阶梯是到顶、在长阶梯才过半 —— 该包飞升率 0% 对 64%，而报表上只显示成一个数。新增 `realm_top` 表达"距绝顶几层"，`realm_index_absolute` 门禁盯着这个。

---

## 二、📐 跨会话契约（代码级，必须保持一致）

### 目录与边界

```
src/core/       纯 TS 引擎 —— **零浏览器 API、零 DOM、零 React、零 Node API**
  绝不可 import：react / node:* / document / window / localStorage / fetch
  由 `npm run check:core` 强制。破坏它 = 蒙特卡洛模拟跑不了。
src/content/    全部内容 JSON（构建期确定，运行时只读）
src/ui/         React 视图层
tools/          门禁与验证工具
electron/       Electron 壳（主进程持有 API Key）
```

### 关键不变量

| 项 | 规则 |
|---|---|
| `hp` | **伤势**：0 = 完好，100 = 油尽灯枯。正 delta = 受伤加重。（与"血量"相反） |
| `lifespan` | **寿元折损**，不是年龄也不是剩余寿命。死活判据 = `年龄 >= 境界寿命 + 折损` |
| `power` | 累积修为（内部累加器），`power_index = power × POWER_TO_INDEX`（当前 0.26） |
| 属性 | 开局 40 起。**一局内可达区间：典型 43 / 上限 58**（`ATTR_TYPICAL` / `ATTR_REACHABLE`） |
| 判定 | 四段式 crit / success / fail / crit_fail，模糊提示不用精确数字 |
| 相性 | 气→体→灵→则→意→气 相克环；**势不入环** |
| 本体映射 | mortal/genius=气 · physique=体 · mystery=灵 · rebel=意 · cautious=势 |
| 渲染 | 叙事一律**第二人称「你」**，3 行、每行 ≤60 字 |
| 文本禁区 | 不得出现阿拉伯数字、资源名、属性名、系统词、现实品牌、外链 |

### 端口与命令

| 命令 | 作用 |
|---|---|
| `npm run dev` | 本地开发（5173） |
| `npm test` | 单元测试 |
| `npm run lint:content` | **内容门禁**（IP / 曲线 / 剧本科解性 / 正文可达 / 属性可达 / hp 语义 / 阶段覆盖） |
| `npm run check:core` | 核心纯度 |
| `npx tsx tools/sim.ts N` | 蒙特卡洛平衡验证 |
| `npx tsx tools/playtest.ts N` | 试玩 agent，产出 `playtest-findings.md` |
| `npm run dist:win` | 打 Windows exe（**必须在 Windows 上跑**） |

部署：推 `main` → GitHub Actions 跑完整门禁 → GitHub Pages（https://cryts.github.io/xuan/）。
**任一门禁失败就不上线**，这是故意的。

---

## 三、⚠️ 已知陷阱（踩过的坑，别再踩）

- [ ] **两套内容加载器**：`tools/load-content.ts`（Node，走 fs）与 `src/ui/store.ts`（浏览器，走 `import.meta.glob`）。改内容格式时**两边都要改**，`tests/loader-parity.test.ts` 会看着。
- [ ] **`bindContent(content)` 必须调用**，否则 `add_item` 全是空操作。UI 与 tools 都要调。
- [ ] **加了新的 JSON 文件要确认加载器认它**（`l2*.json` 是自动并入的，其它要显式接）。
- [ ] **剧本属性门槛别超 58** —— 超了就是死路，闸门会拦。
- [ ] **`scenario_solvable` 的短路洞已修**，现在会全量校验引用。
- [ ] **新增"专用面板"类型的节点时，要往 `tools/playtest.ts` 的 `hasPanel()` 里加一项。** 它列出的就是"玩家能操作的面"；漏加会让试玩 agent 把该节点当成空节点**强制跳过**，而且不报错。
- [ ] **`realm_top` 依赖 `ctx.realmsTotal`。** 谁调用 `evaluate` / `fitScore` / `satisfactionScore` 而没传它，`realm_top` 就对所有人判否 —— 静默地一个结局都拿不到。`resolveEnding` 已传，新加调用点要照做。
- [ ] **`buildContentDB()` 在 `tools/load-content.ts`**，不要在工具里另抄一份（"两套加载器漂移"是这个项目吃过亏的事）。
- [ ] **别用 `git add -A`** —— 会把并行 agent 或用户正在写的文件一起提交。用显式路径。
- [ ] **命令别用 `;` 串** —— 构建失败拦不住后面的 commit && push。用 `&&`。

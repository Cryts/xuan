# 《玄》内容构造指南

> 本文件是**内容生产 agent 的唯一规范**。类型定义见 `src/core/types.ts`，设计依据见 `docs/superpowers/specs/2026-09-20-xuan-design.md`。
> 违反本指南的内容会被 `tools/content-lint.ts` 打回。

---

## 0. 三条不可违背的铁律

1. **每个事件必须至少改写一个长期变量。** 长期变量 = `currency / power / rare_mat / favor / debt / exposure / corruption / karma / hp / lifespan`。只改属性（attrs）不算。
2. **不得存在支配性选项。** 四个选项不能是收益 10/20/30/40 的线性排列。每个选项应改写**不同的**长期变量。
3. **不得使用任何原著独有名词。** 见第 6 章禁用词表。命中即构建失败。

---

## 1. 六体系包与本体的对应

| 包 ID | 原创名 | 本体 | 玩法原型 | 独有机制槽 | 失败方式 |
|---|---|---|---|---|---|
| `mortal` | 青冥仙途 | **气** | 慢热经营 | `time_skip` `alchemy` | 资源断档、被夺宝追杀 |
| `genius` | 炎武纪元 | **气** | 热血升级 | `flame_rank` `alchemy_rank` | 焚身、势力追杀 |
| `physique` | 太古遗蜕 | **体** | 高强度成长 | `bone_inscription` `era` | 肉身崩解、纪元清算 |
| `mystery` | 灰雾之秘 | **灵** | 解谜扮演 | `sanity` `aggregation` `path_lock` | 失控、聚合者追杀、身份暴露 |
| `rebel` | 逆天问道 | **意** | 长线杀伐 | `will` `tribulation` | 天劫反噬、道心崩碎 |
| `cautious` | 苟道长生 | **势** | 低风险求生 | `backup_plan` `conceal` | 暴露后清算、后手耗尽 |

**同一母题在不同体系包下必须换风险类型**（SPEC 5.2 硬约束 #3）：
- 秘境夺宝 → 凡人流偏**资源断档**；诡秘流偏**封印物污染**；高武流偏**规则压制**。

---

## 2. 事件 Schema（散事件）

见 `src/core/types.ts` 的 `LooseEvent`。要点：

- `id`：`evt_<pack>_<motif短名>_<序号>`
- `narrative.body_key`：L1/L2 寻址键，形如 `loose.<motif>.<stage>`
- `options`：1–4 个。选项数触发规则：
  - **1 个** = 命运已定（演出型，无 `resolve`，走 `outcome`）
  - **2 个** = 两难（保命 vs 夺宝、正道 vs 魔道）
  - **3 个** = 标准（激进 / 稳健 / 放弃），占约 55%
  - **4 个** = 高信息量场面（拍卖会、宗门大比、阵营站队），占约 15%
- `options[].intent` 枚举：`greedy | steady | scheme | flee | evil | social | study | sacrifice`
- `risk_tier` 五档：`稳 | 常 | 险 | 狠 | 绝`
- `odds_hint` 模糊概率：`十拿九稳 | 有把握 | 未卜 | 凶险 | 九死一生`
- `resolve.roll.base` 基准 0.55 左右，`attr` + `attr_weight` 做属性加成
- `bands` 四段：`crit`（5–10%）/ `success`（45–55%）/ `fail`（25–35%）/ `crit_fail`（10–15%）。**不必四段都写**，缺的段回落到最近段。

### 效果类型（`effects[].type`）

`add_var` / `add_attr` / `add_item` / `consume_item` / `add_affix` / `set_flag` / `clear_flag` /
`set_relation` / `queue_followup` / `force_stage_jump` / `unlock_title` / `unlock_codex` /
`learn_rule` / `trigger_end` / `modify_power_index` / `destiny_drain`

---

## 3. 剧本 Schema —— 本作的核心内容形态

见 `src/core/types.ts` 的 `Scenario`。

### 3.1 明规则 / 隐规则的分工

> **明规则定义「做什么 / 做不到什么」，隐规则定义「为什么 / 怎么绕」。**

- `rules_stated`：开局**全部告知**玩家。文本量要小（2–4 条），每条一句话。
- `rules_hidden`：靠属性 / 知识 / 道具揭示，UI 显示为「未解」。这是推理动机的来源。
  每条含 `id` / `hint`（揭示后显示）/ `reveal`（一个 `Condition`）。

**好隐规则的判据**：它揭示后，会让玩家重新理解明规则。典型是**字面漏洞**——
例：明规则说「禁绝灵力」，隐规则揭示「禁的是『调用』，不是『已有』」。

### 3.2 多条件破局（`breakthroughs`）

- 每个剧本 **3–6 组**破局条件组，**互不包含**（不能一组是另一组的子集）。
- 条件类型必须 **≥3 种**（不能全是 `item`/`affordance`）。
- 每组有独立的 `outcome.ref`（结局线索 ID），**达成哪一组决定走向哪个结局**。
- 允许一组 `hidden: true`，条件是 `solved_any_of`（组合涌现：先通两条路，再开第三条）。

**13 种条件类型**（`Condition`）：
`attr` / `var` / `item` / `affordance` / `affix_count` / `known_rule` / `learned_rule` /
`flag` / `relation` / `faction_tier` / `solved_any_of` / `node_count` / `realm_idx` / `pack`
+ 逻辑组合 `any` / `all` / `not`

**`affordance` 是涌现的关键**：它只认**功能标签**，不认具体物品。场景挂 `镇魂` 槽，则任何带 `镇魂` 标签的物品（含未来新增的）都自动成为合法解法。**设计师写条件，不写解法。**

### 3.3 `unsolved` 不是失败

未破局必须写成一**独立结局**（被封在冢中成了传说），而不是"操作无效"。
参考极乐迪斯科：失败是分支内容，不是惩罚。

### 3.4 可复用的剧本原型（来自《惊悚乐园》方法论调研）

`span` 3–8 节点。以下 14 个原型可直接取材，**必须改造成修仙语境**：

| # | 原型 | 修仙化方向 |
|---|---|---|
| T01 | 限时合成 | 限时炼丹/解毒；死亡次数=续命次数 |
| T02 | 强制分离通讯 | 传音符只能单向限时；漏接者被追杀 |
| T03 | 速通换难度 | 越早到某地难度越低；踩中 FLAG 失去关键助力 |
| T04 | 精神世界/人格归因 | 心魔境；全部 NPC 是主角人格投射 |
| T05 | 绝对规则博弈 | 论道台：先列出硬规则，再找对手能力边界 |
| T06 | 定义漏洞机关 | 上古器物：输入概念即实体化，排除定义不明项 |
| T07 | 主观判定规避 | 天道的"消极避世"判定看主观意图 |
| T08 | 信念改写现实 | 「信以为真即成真」的秘境，需低成本命题试错 |
| T09 | 元叙事预算 | 谶言：事件须恰在第 N 章节结束 |
| T10 | 冒名系统 | 天道敕令是第三方伪造；真天道只能靠改任务名暗示 |
| T11 | 多人对抗积分 | 大比积分；借势击杀只得 1 分 |
| T12 | 评分-难度绑定 | 天机评价越高，下一劫越重；评价有滞后性 |
| T13 | 封闭凶案多重解答 | 宗门命案；线索分绝对真/条件真/可能假三级 |
| T14 | 阵营多结局 | 正魔站队；隐藏数值累计到阈值解锁隐藏结局 |

### 3.5 解题范式（写选项时参照，来自封不觉）

1. **规则层级识别**——先分清"绝对规则"与"可议价条款"
2. **能力边界测绘**——把敌人能力当"带参数的功能"，找它的参数边界
3. **字面/定义漏洞**——规则的字面 vs 意图
4. **把判定标准指向主观**——判定看意图时，改变意图即可
5. **把"相信"变成资源**——信念实体化的世界
6. **把时间当可议价变量**——快则难度低，慢则雪球
7. **把恐怖源拆成可操作条件**——荒诞弱点即突破口
8. **借力打力**——把 NPC 与势力变成免费劳动力
9. **绕过而非解开**——笨办法（地毯式搜索）也是解法
10. **不击杀而转化**——解放而非杀死，换取情报

---

## 4. 位面之子素材

每个体系包需提供：
- `fateTemplates[pack]`：该体系的命运线里程碑池（`FateMilestone[]`），≥6 条。每条含 `name` / `narrative` / `power_gain` / 可选 `world_effect`。
- `oraclePool`：谶语式「天机批注」池，≥30 条。风格：克制、留白、似偈非偈。
- `coincidencePools`：天意庇佑的"合理巧合"叙述池（L2），按原型分：
  - `destiny.protect.brute` / `.schemer` / `.turtle` / `.ironic` / `.tragic`
  - `destiny.protect.resist.<archetype>`（智谋型抗性触发时）
  - `destiny.protect.none`
  每个 key ≥4 条，每条 2–3 行、每行 ≤60 字。

**巧合必须"合理"**：护道人及时赶到 / 天雷劈偏杀招 / 你的仇家恰在此刻杀来 / 他体内残魂醒了一瞬。
不能写成"主角光环发作"，要写成**看起来像是巧合、实则是天道在改剧本**。

---

## 5. 叙事文本层

| 层 | 内容 | 谁写 |
|---|---|---|
| L0 | 规则文本、提示 | 手写 |
| L1 | 变量槽模板 | 手写模板 + 运行时填充 |
| **L2** | **预生成候选池** | **内容 agent 批量生成（本节）** |
| L3 | 结局卷轴 | 运行时由玩家自填 key 调用 |

**L2 寻址键**：`hash(evt_id, band, pack, realm_bucket, dominant_attr, mood_hint)`
实际写入时用 `narrative.body_key`，形如 `loose.<motif>.<stage>`，
每个 key 给 **3–5 条候选**（运行时按种子选一条，并做玩家级 30 节点去重）。

**L2 文本硬约束**：
- 每段 3 行，每行 ≤60 字
- **禁止出现任何阿拉伯数字、资源名、属性名、成功率、系统词**
- 禁止现实品牌、联系方式、外链
- 古风一致，不混入其他体系的术语
- `mood` 枚举：`eerie | heroic | somber | comic | mystic | tense`

---

## 6. 禁用词表（命中即 build fail）

**通用修真词可保留**：炼气 / 筑基 / 金丹 / 元婴 / 化神 / 天劫 / 秘境 / 丹药 / 灵石 / 灵气 / 法宝 / 妖兽 / 宗门

**以下必须 100% 替换**（节选，完整表在 `tools/forbidden-words.ts`）：

凡人：掌天瓶、韩立、墨大夫、升仙大会
斗破：萧炎、药老、焚诀、骨灵冷火、青莲地心火、陨落心炎、魂殿、纳戒、魔核、斗帝
武动：林动、石符、祖符、异魔、位面之主
完美：石村、柳神、至尊骨、原始真解、骨文、他化自在大法、荒域
遮天：荒古圣体、万物母气、九秘、成仙路、红尘仙、帝兵、东荒
莽荒：应龙血脉、生死道君、永恒帝君、世界神
雪中：北凉、听潮亭、李淳罡、气运金龙
将夜：昊天、书院二层楼、知守观、夫子、无矩
诡秘：愚者、塔罗会、灰雾之上、值夜者、封印物、源质、旧日、外神、序列途径名、克莱恩、扮演法、成神仪式
宿环：因蒂斯、沉睡克莱恩、外神序列
道诡：福生天、白玉京、大傩
诛仙：太极玄清道、大梵般若、噬血珠、七脉会武、青云山、碧瑶、诛仙剑阵
仙逆：王林、恒岳派、尸阴宗、踏天九桥、天人五衰
一念：长生卷、不死长生功、罗天
飞剑：法眼、碧游宫、本命飞剑
星辰：秦羽、流星泪、潜龙大陆、鸿蒙、四九小天劫、仙魔妖界
吞噬：陨墨星、巴巴塔、金角巨兽、基因原能、原始宇宙权限
师兄：李长寿、纸道人、鸿蒙紫气
深空：旧术、新术、内景地、元神圣物、彼岸、全领域六破

**替换示例**：掌天瓶 → 催熟残瓶 / 青冥瓶；异火榜 → 炎灵谱 / 天火录；
魂殿 → 幽冥殿；塔罗会 → 灰雾议团；至尊骨 → 太古骨篆；序列 9 → 阶位九品。

---

## 7. 原创名生成规则

- 姓：沈/陆/姜/云/裴/燕/竺/容/谢/萧/顾/苏/百里/第五/澹台
- 名：砚/舟/珩/澈/寻/照/野/辞/度/青崖/长庚/无咎/怀瑾/惊鸿/临渊/寒山/明烛/折雪/归尘
- 道号：`{element}{noun}{suffix}`，如 玄冥子 / 太一真人
- 宗门：`{place}{sect_suffix}`，如 云隐宗 / 青冥观
- 法宝：`{prefix}{material}{type}`，如 残缺·血沁·青铜剑

**约束**：≤4 字；禁现实品牌；禁不雅谐音；全局去重。

---

## 8. 输出位置

```
src/content/
├── packs/{mortal,genius,physique,mystery,rebel,cautious}.json
├── terms/{...}.json
├── meta/{origins,traits,flaws,destinies}.json
├── motifs.json
├── events/{childhood,entry,growth,turn,endgame}.json
├── scenarios/*.json
├── endings.json
├── narrative/l2.json          # { "<key>": ["...", "..."] }
├── narrative/oracle.json
├── narrative/coincidence.json
├── names.json
└── items/{items,affixes}.json
```

全部 JSON 用 UTF-8，缩进 2 空格，**顶层是数组或对象，不要包注释**。

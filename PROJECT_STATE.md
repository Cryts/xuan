# 变更日志 PROJECT_STATE

> 记录所有影响项目走向的决策和当前任务状态。
> 最新条目在最上面，已完成 ✅，进行中 🚧，阻塞 🔴。
> 最近更新：2026-09-20 18:20

---

## 🚧 当前任务

**正在做**：把「剧本是前面一路选择的收束」落地 —— 前置链调度（#29）与条件随进度缩放（#30）。

- #29 的现状：铺垫事件**在内容里存在**（145 处 `add_item`，其中 22 个事件没有任何东西调度它），但**没有任何一条指向剧本**（`queue_followup` 指向 `scn_*` 的数量 = 0），引擎侧也没接。缺的是两头。
  → 定下的接口：`Scenario` 增加 `leadup_motifs: string[]`，引擎在 `advanceNode` 里规划链（`advanceNode` 是唯一能安全改状态的地方；`pickNextEvent` 被多次重入，不能在那里改状态）。
- #30 的现状：门槛仍是固定值。`realm_top`（距绝顶几层）刚做好，正好是这一条的地基。
- 阻塞点：无。
- 预期下一步：先定 `Scenario` 的字段与 `advanceNode` 的规划逻辑，再把 18 个剧本的 motif 映射派给 agent。

**上一批（已推送 `86ddfe1`）**：试玩仪表盲区、游历死选项、支配性 87%→17.6%、飞升条件绝对序号、选项预告派生。

---

## 2026-09-20

### 修复（最近的在上）

- ✅ **试玩 agent 的仪表瞎了 —— 三百局没走过一次日常、没打过一场斗法。** 空节点判定跑在 `daily`/`duel` 分支**之前**并 `continue`，于是 `__daily__` / `__duel__` / `__duel_after__`（三者 `options` 都是空数组，由专用面板承接）被当成"卡死"强制推节点。findings 里 **4522 次"空节点"全是误报**，而 #31 的支配性正是这个工具量的。修完：空节点 4522 → 0，事件覆盖 451 → 488/553，剧本 12 → 18/18。判据提为 `hasPanel()`，列在一处，以后加专用面板往那里加。
- ✅ **「游历」是死选项。** 注释写着"给一笔小进项，免得'出门'变成纯粹的零收益选项"，函数体给的是 `delta: []` —— 界面上写着"机缘与凶险都在里头"，实际恒等于跳过这一拍，被其余四项严格支配。`last_daily` 也是写了两处、读零处。补上小进项，并把张力期待拉满接到调度器（挑具体标签会随内容漂移，张力是每事件都有的数据轴）。
- ✅ **支配性 87% → 17.6%。** 消融实验定位：关掉日常闭关收益，三原型战力差从 67% 掉到 **16%**。逐条修：伤势乘子线性→开方（线性意味着半伤扣半修为，而修为要跨二十多个节点积分，一次受伤变成永久税）；自愈基数 1.2→3.0（枢纽里只有闭关能疗伤，"受伤就没得选"才是不均等的实质）；闭关乘子 4.2→2.5、递减改按一局累计（原按连续，中间插一次别的就归零，从不生效）。
- ✅ **飞升条件是绝对序号，各包阶梯长短不一。** `realm_idx >= 9` 在灰雾之秘（10 层）是到顶、在太古遗蜕（18 层）才过半：该包飞升率 **0% 对 64%**，全体 25% 几乎全是长阶梯的包在灌水。新增 `realm_top`（距绝顶几层），转换 6 处。
- ✅ **缺一个「干净登顶」的结局。** 五个飞升结局各要一样代价（入魔/暴露/欠债/无亲无友/负因果），唯独没给什么都没欠的人留位置 —— 清净修到顶的玩家无结局可落，被兜底算成道陨。补 `end_ascend_clean`。
- ✅ **选项预告字段做了、界面渲染了、内容一条没有**（1629 个选项 `gain_hint` 0 / `path_hint` 0）。改为从 `resolve.bands` / `outcome.effects` 派生：覆盖 1621/1629，两者皆无 **0 个**。
- ✅ **采药不再是净伤。** 自愈抬高后脚力被自然自愈吃掉，界面上"伤势略增"成了假话 —— 提示与行为对不上比数值偏一点更糟。
- ✅ **`scenario_solvable` 门禁有短路洞，已修但暴露出 32 处内容错误。** `triOf` 对数组用了会短路的 `some`：只要第一个条件是 possible，后面的 `affordance / item / learned_rule` 引用**根本不会被校验**。修成先全量求值再合并后，立刻报出 32 处失效引用（此前全绿）。**门禁里最危险的不是判错，是没判。**
- ✅ **`add_item` 是空操作 —— 整个道具经济从未运转。** 原型从 `state.items`（玩家自己的背包）里找，而不是从内容目录里找；玩家本来就没有那件，`find` 返回 undefined、效果**静默丢弃**。修法：加模块级 `ITEM_CATALOG` / `AFFIX_CATALOG`，由 `bindContent(content)` 在加载内容时建立；UI 与 tools 都已接。附 7 条回归测试。
- ✅ **剧本属性门槛在数学上够不着。** 12 个剧本里 48 个 `attr` 门槛写着 85（中位 70），而实测 200 局：属性中位 **43**、九十分位 47、全场最高 **54**。全部死路。72 处重标为 44~52（`40 + (旧−40)×0.25`）。新增 `scenario_attr_reachable` 门禁 + 把实测值固化进 `genesis.ts` 的 `ATTR_TYPICAL`/`ATTR_REACHABLE`。
- ✅ **开局伤势恒满。** 13 条出身的 `start_resources.hp` 写着 80~100（"开局气血"的旧语义），而 `hp` 现在是**伤势**，于是"建号即垂危"、狼裔那条 `hp:100` 直接开局判死。按出身苦楚程度重映射为 0~25。新增 `origin_hp` 门禁。
- ✅ **浏览器侧内容加载器坏了两处**：`packs: base.packs`（恒取兜底，真实内容从未生效——线上一直跑在占位包上）与 `isAffix` 多写了 `!Array.isArray(o.effects)`（89 条词条只剩 29 条）。新增 `tests/loader-parity.test.ts` 逐字段比对两套加载器。
- ✅ **结局正文 64/64 全部缺失。** 玩家打完一世永远只看到兜底那句"一世终了"。补全 + 新增 `text_resolvable` 门禁。
- ✅ **`hp` 语义被两批 agent 写反**（一批按"血量"写）。新增 `hp_semantics` 门禁（判据用 `fail` 段，不用 `success` 段）。
- ✅ **13 处剧本隐藏通路不可达**：一命中破局组就退出剧本，`solved` 永远只有一个元素，`solved_any_of` 全是死内容。改为记录但**不立即退出**。
- ✅ **调度器被自己的冷却规则锁死**：全部童年事件都带 `early` 标签，一个触发后整池互相排斥，游戏从此空转。加三级放宽安全网 + 结构性标签（数据驱动，不写死清单）。
- ✅ **保底队列不出队**：窗口 `[7,12]` 的连锁事件在第 7~12 拍**每一拍都返回同一个事件**，绕过全部冷却与权重。

### 已完成的系统（本次会话）

- ✅ **斗法**：明牌比试 + 路数对抗，复用六本体相性环。归位为**奇遇**（遭遇 → 出手/避开 → 架势 → 杀/放）。杀死是独立选择，有独立奖惩。修了两处自身缺陷：`defense` 写成乘法（守势反而挨打更多，稳守负率 97%）→ 改除法；架势原本分"攻守"是**假取舍**（斗法是竞速，"削他"与"涨我"同一根轴）→ 改为**期望相当、起伏不同**。
- ✅ **日常行动**：每 4 拍插一次，五项目（闭关/游历/采药/坊市/交游）。闭关有递减（闭门造车），防止"每次都闭关"成支配策略。
- ✅ **修为与处境挂钩**：按最近一手的性质给宽幅系数（参悟 1.0 → 逃命 0.25），伤势以 85 为零点（垂危转负=倒退），心魔与因果拖慢。
- ✅ **物品分家**：`daily` 可随手使用（**不推进节点**）、`key` 只在局里使得上。分类由功能标签派生，不靠人标。
- ✅ **小境界**：大境界内分层（炼气九层 / 筑基初期中期后期圆满），序列类体系（灰雾之秘）可显式关闭。
- ✅ **剧本松绑**：入场抉择（可绕开）、六种通用手段（细察/交涉/硬闯/感气/静待/抽身）、未破局不再终结一世。
- ✅ **剧本多样化**：新增 6 个非解谜剧本 —— 逐斗 / 逃亡 / 守御 / 渡劫 / 博弈 / 规则压制（剧本 12 → 18）。
- ✅ **自由输入分支（实验，默认关闭）**：意图落地判定 + 四个 Provider（native→ollama→api→rule）。核心是**玩家提到的东西必须真的在他身上**，否则落不了地。
- ✅ **Electron 壳**：密钥存主进程、由主进程发请求 —— 一举解决网页版做自由输入的两个死结（密钥明文在前端、国内模型不发 CORS 头）。
- ✅ **试玩 agent**（`tools/playtest.ts`）：会琢磨的玩家，自动挑毛病并累积成 `playtest-findings.md`。

### 已采纳的设计决定（用户明确采纳的）

- ✅ **斗法两段式**：明牌负责"赌不赌"与带什么进场，路数对抗负责"怎么打"。**已实现。**
- ✅ **剧本不再是孤立的谜题，而是前面一路选择的收束**：条件随进度缩放、前置链产出剧本道具、剧本声明工具箱、开场文本给线索。**尚未实现。**
- ✅ **物品分日常/剧本关键两类，日常可随时使用。** 已实现。
- 🚧 **选项后果预告从模糊改具体。** 方向已定但未做——《杀戮尖塔》的教训（Mega Crit 先做了模糊预告然后明确否掉，Yano 原话"实际上改完更糟糕，玩家需要考虑的更多了"；Giovannetti"我完全不知道结果会是什么，只能去 Google，这永远是游戏设计的彻底失败"）。现有的 `gain_hint`/`path_hint` 字段与卡片展示已做好，但内容还没填（约 1900 个选项）。

### 未完成（按建议顺序）

- 🚧 **前置链调度**（#29）：见【当前任务】。
- 🚧 **条件随进度缩放 + 剧本工具箱声明**（#30）。
- 🚧 **剧本开场文本覆盖**：18 个剧本的 intro 已补（`l2_scenario_intro.json` 与 `l2_scenario_new.json` **有 6 个 key 撞车**，后者覆盖前者，需二选一删除）。
- 🚧 **隐藏规则的 narrative key 无处可挂**：`Scenario.rules_hidden` 只有 `id/hint/reveal`，没有 narrative 字段；agent 写了 17 条揭示文本但引擎不查。
- 🚧 **`rare_mat` 有进无出**：内容里 **772 进 / 57 出**，材料基本没有消耗口 —— 「采药」这条日常因此也接近死选项（与「游历」同类，只是没游历那么彻底）。
- 🚧 **`favor` 是窄轴**：内容里 617 进 / 525 出，但全库只有 **12 处**把它当判据。谋士原型（交游→声望）因此垫底（216 对稳健 256）。
- 🟡 **道陨类 39.9%，贴 40% 的线**。其中纯「道陨」31.0%，另 7.1% 是「因果清算」、1.7% 是「以身殉道」—— 后两者被验收正则 `/道陨|入魔|殉道|清算/` 一并算作坏结局。**这两个是否该算坏结局，是个设计问题，不该由我改正则来"通过"。**
- 🚧 **从未抽到的事件 65 个（12%）**，按阶段：growth 37 · turn 27 · endgame 8。数量偏多，说明权重或前置把它们压死了。

### 环境

- ✅ **联网需代理**：`export http_proxy/https_proxy=http://10.230.33.86:7890`（git 推送走 SSH，本来就通）。建议一并设 `no_proxy=localhost,127.0.0.1,::1`，否则本地 vite dev 会绕道。
- ⚠️ **调研 agent 大面积失败**：Bing RSS 返中文词典垃圾、so.com 挂了、B站限流、gamasutra/kotaku/polygon/reddit/web.archive.org 不可达。活下来的两份靠 sitemap 挖 + curl。**再派调研 agent 前先确认代理已设。**
